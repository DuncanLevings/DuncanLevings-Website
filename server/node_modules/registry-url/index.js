'use strict';
const rc = require('rc');

const registryUrl = scope => ***REMOVED***
	const result = rc('npm', ***REMOVED***registry: 'https://registry.npmjs.org/'***REMOVED***);
	const url = result[`$***REMOVED***scope***REMOVED***:registry`] || result.config_registry || result.registry;
	return url.slice(-1) === '/' ? url : `$***REMOVED***url***REMOVED***/`;
***REMOVED***;

module.exports = registryUrl;
// TODO: Remove this for the next major release
module.exports.default = registryUrl;

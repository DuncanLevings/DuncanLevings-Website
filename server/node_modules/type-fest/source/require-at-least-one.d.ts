import ***REMOVED***Except***REMOVED*** from './except';

/**
Create a type that requires at least one of the given keys. The remaining keys are kept as is.

@example
```
import ***REMOVED***RequireAtLeastOne***REMOVED*** from 'type-fest';

type Responder = ***REMOVED***
	text?: () => string;
	json?: () => string;

	secure?: boolean;
***REMOVED***;

const responder: RequireAtLeastOne<Responder, 'text' | 'json'> = ***REMOVED***
	json: () => '***REMOVED***"message": "ok"***REMOVED***',
	secure: true
***REMOVED***;
```
*/
export type RequireAtLeastOne<ObjectType, KeysType extends keyof ObjectType = keyof ObjectType> =
	***REMOVED***
		// For each Key in KeysType make a mapped type
		[Key in KeysType]: (
			// …by picking that Key's type and making it required
			Required<Pick<ObjectType, Key>>
		)
	***REMOVED***[KeysType]
	// …then, make intersection types by adding the remaining keys to each mapped type.
	& Except<ObjectType, KeysType>;

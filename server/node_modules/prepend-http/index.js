'use strict';
module.exports = (url, opts) => ***REMOVED***
	if (typeof url !== 'string') ***REMOVED***
		throw new TypeError(`Expected \`url\` to be of type \`string\`, got \`$***REMOVED***typeof url***REMOVED***\``);
	***REMOVED***

	url = url.trim();
	opts = Object.assign(***REMOVED***https: false***REMOVED***, opts);

	if (/^\.*\/|^(?!localhost)\w+:/.test(url)) ***REMOVED***
		return url;
	***REMOVED***

	return url.replace(/^(?!(?:\w+:)?\/\/)/, opts.https ? 'https://' : 'http://');
***REMOVED***;

'use strict';
const packageJson = require('package-json');

const lastestVersion = async (packageName, options) => ***REMOVED***
	const ***REMOVED***version***REMOVED*** = await packageJson(packageName.toLowerCase(), options);
	return version;
***REMOVED***;

module.exports = lastestVersion;
// TODO: Remove this for the next major release
module.exports.default = lastestVersion;

'use strict';
const is = require('@sindresorhus/is');

module.exports = url => ***REMOVED***
	const options = ***REMOVED***
		protocol: url.protocol,
		hostname: url.hostname.startsWith('[') ? url.hostname.slice(1, -1) : url.hostname,
		hash: url.hash,
		search: url.search,
		pathname: url.pathname,
		href: url.href
	***REMOVED***;

	if (is.string(url.port) && url.port.length > 0) ***REMOVED***
		options.port = Number(url.port);
	***REMOVED***

	if (url.username || url.password) ***REMOVED***
		options.auth = `$***REMOVED***url.username***REMOVED***:$***REMOVED***url.password***REMOVED***`;
	***REMOVED***

	options.path = is.null(url.search) ? url.pathname : `$***REMOVED***url.pathname***REMOVED***$***REMOVED***url.search***REMOVED***`;

	return options;
***REMOVED***;

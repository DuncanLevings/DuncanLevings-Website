'use strict';
const is = require('@sindresorhus/is');

module.exports = function deepFreeze(object) ***REMOVED***
	for (const [key, value] of Object.entries(object)) ***REMOVED***
		if (is.plainObject(value) || is.array(value)) ***REMOVED***
			deepFreeze(object[key]);
		***REMOVED***
	***REMOVED***

	return Object.freeze(object);
***REMOVED***;

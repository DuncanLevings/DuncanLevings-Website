'use strict';
const url = require('url');
const prependHttp = require('prepend-http');

module.exports = (input, options) => ***REMOVED***
	if (typeof input !== 'string') ***REMOVED***
		throw new TypeError(`Expected \`url\` to be of type \`string\`, got \`$***REMOVED***typeof input***REMOVED***\` instead.`);
	***REMOVED***

	const finalUrl = prependHttp(input, Object.assign(***REMOVED***https: true***REMOVED***, options));
	return url.parse(finalUrl);
***REMOVED***;

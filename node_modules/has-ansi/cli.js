#!/usr/bin/env node
'use strict';
var pkg = require('./package.json');
var hasAnsi = require('./');
var input = process.argv[2];

function stdin(cb) ***REMOVED***
	var ret = '';
	process.stdin.setEncoding('utf8');
	process.stdin.on('data', function (data) ***REMOVED***
		ret += data;
	***REMOVED***);
	process.stdin.on('end', function () ***REMOVED***
		cb(ret);
	***REMOVED***);
***REMOVED***

function help() ***REMOVED***
	console.log([
		pkg.description,
		'',
		'Usage',
		'  $ has-ansi <string>',
		'  $ echo <string> | has-ansi',
		'',
		'Exits with code 0 if input has ANSI escape codes and 1 if not'
	].join('\n'));
***REMOVED***

function init(data) ***REMOVED***
	process.exit(hasAnsi(data) ? 0 : 1);
***REMOVED***

if (process.argv.indexOf('--help') !== -1) ***REMOVED***
	help();
	return;
***REMOVED***

if (process.argv.indexOf('--version') !== -1) ***REMOVED***
	console.log(pkg.version);
	return;
***REMOVED***

if (process.stdin.isTTY) ***REMOVED***
	if (!input) ***REMOVED***
		help();
		return;
	***REMOVED***

	init(input);
***REMOVED*** else ***REMOVED***
	stdin(init);
***REMOVED***

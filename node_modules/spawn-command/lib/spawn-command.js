var util = require('util');
var spawn = require('child_process').spawn;

module.exports = function (command, options) ***REMOVED***
  var file, args;
  if (process.platform === 'win32') ***REMOVED***
    file = 'cmd.exe';
    args = ['/s', '/c', '"' + command + '"'];
    options = util._extend(***REMOVED******REMOVED***, options);
    options.windowsVerbatimArguments = true;
  ***REMOVED***
  else ***REMOVED***
    file = '/bin/sh';
    args = ['-c', command];
  ***REMOVED***
  return spawn(file, args, options);
***REMOVED***;

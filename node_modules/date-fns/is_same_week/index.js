var startOfWeek = require('../start_of_week/index.js')

/**
 * @category Week Helpers
 * @summary Are the given dates in the same week?
 *
 * @description
 * Are the given dates in the same week?
 *
 * @param ***REMOVED***Date|String|Number***REMOVED*** dateLeft - the first date to check
 * @param ***REMOVED***Date|String|Number***REMOVED*** dateRight - the second date to check
 * @param ***REMOVED***Object***REMOVED*** [options] - the object with options
 * @param ***REMOVED***Number***REMOVED*** [options.weekStartsOn=0] - the index of the first day of the week (0 - Sunday)
 * @returns ***REMOVED***Boolean***REMOVED*** the dates are in the same week
 *
 * @example
 * // Are 31 August 2014 and 4 September 2014 in the same week?
 * var result = isSameWeek(
 *   new Date(2014, 7, 31),
 *   new Date(2014, 8, 4)
 * )
 * //=> true
 *
 * @example
 * // If week starts with Monday,
 * // are 31 August 2014 and 4 September 2014 in the same week?
 * var result = isSameWeek(
 *   new Date(2014, 7, 31),
 *   new Date(2014, 8, 4),
 *   ***REMOVED***weekStartsOn: 1***REMOVED***
 * )
 * //=> false
 */
function isSameWeek (dirtyDateLeft, dirtyDateRight, dirtyOptions) ***REMOVED***
  var dateLeftStartOfWeek = startOfWeek(dirtyDateLeft, dirtyOptions)
  var dateRightStartOfWeek = startOfWeek(dirtyDateRight, dirtyOptions)

  return dateLeftStartOfWeek.getTime() === dateRightStartOfWeek.getTime()
***REMOVED***

module.exports = isSameWeek

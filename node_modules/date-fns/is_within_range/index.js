var parse = require('../parse/index.js')

/**
 * @category Range Helpers
 * @summary Is the given date within the range?
 *
 * @description
 * Is the given date within the range?
 *
 * @param ***REMOVED***Date|String|Number***REMOVED*** date - the date to check
 * @param ***REMOVED***Date|String|Number***REMOVED*** startDate - the start of range
 * @param ***REMOVED***Date|String|Number***REMOVED*** endDate - the end of range
 * @returns ***REMOVED***Boolean***REMOVED*** the date is within the range
 * @throws ***REMOVED***Error***REMOVED*** startDate cannot be after endDate
 *
 * @example
 * // For the date within the range:
 * isWithinRange(
 *   new Date(2014, 0, 3), new Date(2014, 0, 1), new Date(2014, 0, 7)
 * )
 * //=> true
 *
 * @example
 * // For the date outside of the range:
 * isWithinRange(
 *   new Date(2014, 0, 10), new Date(2014, 0, 1), new Date(2014, 0, 7)
 * )
 * //=> false
 */
function isWithinRange (dirtyDate, dirtyStartDate, dirtyEndDate) ***REMOVED***
  var time = parse(dirtyDate).getTime()
  var startTime = parse(dirtyStartDate).getTime()
  var endTime = parse(dirtyEndDate).getTime()

  if (startTime > endTime) ***REMOVED***
    throw new Error('The start of the range cannot be after the end of the range')
  ***REMOVED***

  return time >= startTime && time <= endTime
***REMOVED***

module.exports = isWithinRange

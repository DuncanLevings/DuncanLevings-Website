var parse = require('../parse/index.js')

/**
 * @category Common Helpers
 * @summary Is the first date before the second one?
 *
 * @description
 * Is the first date before the second one?
 *
 * @param ***REMOVED***Date|String|Number***REMOVED*** date - the date that should be before the other one to return true
 * @param ***REMOVED***Date|String|Number***REMOVED*** dateToCompare - the date to compare with
 * @returns ***REMOVED***Boolean***REMOVED*** the first date is before the second date
 *
 * @example
 * // Is 10 July 1989 before 11 February 1987?
 * var result = isBefore(new Date(1989, 6, 10), new Date(1987, 1, 11))
 * //=> false
 */
function isBefore (dirtyDate, dirtyDateToCompare) ***REMOVED***
  var date = parse(dirtyDate)
  var dateToCompare = parse(dirtyDateToCompare)
  return date.getTime() < dateToCompare.getTime()
***REMOVED***

module.exports = isBefore

var parse = require('../parse/index.js')

/**
 * @category Common Helpers
 * @summary Is the first date after the second one?
 *
 * @description
 * Is the first date after the second one?
 *
 * @param ***REMOVED***Date|String|Number***REMOVED*** date - the date that should be after the other one to return true
 * @param ***REMOVED***Date|String|Number***REMOVED*** dateToCompare - the date to compare with
 * @returns ***REMOVED***Boolean***REMOVED*** the first date is after the second date
 *
 * @example
 * // Is 10 July 1989 after 11 February 1987?
 * var result = isAfter(new Date(1989, 6, 10), new Date(1987, 1, 11))
 * //=> true
 */
function isAfter (dirtyDate, dirtyDateToCompare) ***REMOVED***
  var date = parse(dirtyDate)
  var dateToCompare = parse(dirtyDateToCompare)
  return date.getTime() > dateToCompare.getTime()
***REMOVED***

module.exports = isAfter

  (function (schedulerProto) ***REMOVED***
    /**
     * Returns a scheduler that wraps the original scheduler, adding exception handling for scheduled actions.
     * @param ***REMOVED***Function***REMOVED*** handler Handler that's run if an exception is caught. The exception will be rethrown if the handler returns false.
     * @returns ***REMOVED***Scheduler***REMOVED*** Wrapper around the original scheduler, enforcing exception handling.
     */
    schedulerProto.catchError = schedulerProto['catch'] = function (handler) ***REMOVED***
      return new CatchScheduler(this, handler);
    ***REMOVED***;
  ***REMOVED***(Scheduler.prototype));

  function flatMapWithMaxConcurrent(source, maxConcurrent, selector, thisArg) ***REMOVED***
    return source.map(function (x, i) ***REMOVED***
      var result = selector.call(thisArg, x, i);
      isPromise(result) && (result = observableFromPromise(result));
      (isArrayLike(result) || isIterable(result)) && (result = observableFrom(result));
      return result;
    ***REMOVED***).merge(maxConcurrent);
  ***REMOVED***

  /**
   *  One of the Following:
   *  Projects each element of an observable sequence to an observable sequence and merges the resulting observable sequences into one observable sequence.
   *
   * @example
   *  var res = source.flatMapWithMaxConcurrent(5, function (x) ***REMOVED*** return Rx.Observable.range(0, x); ***REMOVED***);
   *  Or:
   *  Projects each element of an observable sequence to an observable sequence, invokes the result selector for the source element and each of the corresponding inner sequence's elements, and merges the results into one observable sequence.
   *
   *  var res = source.flatMapWithMaxConcurrent(5, function (x) ***REMOVED*** return Rx.Observable.range(0, x); ***REMOVED***, function (x, y) ***REMOVED*** return x + y; ***REMOVED***);
   *  Or:
   *  Projects each element of the source observable sequence to the other observable sequence and merges the resulting observable sequences into one observable sequence.
   *
   *  var res = source.flatMapWithMaxConcurrent(5, Rx.Observable.fromArray([1,2,3]));
   * @param selector A transform function to apply to each element or an observable sequence to project each element from the
   * source sequence onto which could be either an observable or Promise.
   * @param ***REMOVED***Function***REMOVED*** [resultSelector]  A transform function to apply to each element of the intermediate sequence.
   * @param ***REMOVED***Any***REMOVED*** [thisArg] Object to use as this when executing callback.
   * @returns ***REMOVED***Observable***REMOVED*** An observable sequence whose elements are the result of invoking the one-to-many transform function collectionSelector on each element of the input sequence and then mapping each of those sequence elements and their corresponding source element to a result element.
   */
  observableProto.selectManyWithMaxConcurrent = observableProto.flatMapWithMaxConcurrent = function (maxConcurrent, selector, resultSelector, thisArg) ***REMOVED***
    if (isFunction(selector) && isFunction(resultSelector)) ***REMOVED***
      return this.flatMapWithMaxConcurrent(maxConcurrent, function (x, i) ***REMOVED***
        var selectorResult = selector(x, i);
        isPromise(selectorResult) && (selectorResult = observableFromPromise(selectorResult));
        (isArrayLike(selectorResult) || isIterable(selectorResult)) && (selectorResult = observableFrom(selectorResult));

        return selectorResult.map(function (y) ***REMOVED***
          return resultSelector(x, y, i);
        ***REMOVED***);
      ***REMOVED***, thisArg);
    ***REMOVED***
    return isFunction(selector) ?
      flatMapWithMaxConcurrent(this, maxConcurrent, selector, thisArg) :
      flatMapWithMaxConcurrent(this, maxConcurrent, function () ***REMOVED*** return selector; ***REMOVED***);
  ***REMOVED***;

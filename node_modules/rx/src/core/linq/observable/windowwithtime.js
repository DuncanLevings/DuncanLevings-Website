  /**
   *  Projects each element of an observable sequence into zero or more windows which are produced based on timing information.
   * @param ***REMOVED***Number***REMOVED*** timeSpan Length of each window (specified as an integer denoting milliseconds).
   * @param ***REMOVED***Mixed***REMOVED*** [timeShiftOrScheduler]  Interval between creation of consecutive windows (specified as an integer denoting milliseconds), or an optional scheduler parameter. If not specified, the time shift corresponds to the timeSpan parameter, resulting in non-overlapping adjacent windows.
   * @param ***REMOVED***Scheduler***REMOVED*** [scheduler]  Scheduler to run windowing timers on. If not specified, the timeout scheduler is used.
   * @returns ***REMOVED***Observable***REMOVED*** An observable sequence of windows.
   */
  observableProto.windowWithTime = function (timeSpan, timeShiftOrScheduler, scheduler) ***REMOVED***
    var source = this, timeShift;
    timeShiftOrScheduler == null && (timeShift = timeSpan);
    isScheduler(scheduler) || (scheduler = timeoutScheduler);
    if (typeof timeShiftOrScheduler === 'number') ***REMOVED***
      timeShift = timeShiftOrScheduler;
    ***REMOVED*** else if (isScheduler(timeShiftOrScheduler)) ***REMOVED***
      timeShift = timeSpan;
      scheduler = timeShiftOrScheduler;
    ***REMOVED***
    return new AnonymousObservable(function (observer) ***REMOVED***
      var groupDisposable,
        nextShift = timeShift,
        nextSpan = timeSpan,
        q = [],
        refCountDisposable,
        timerD = new SerialDisposable(),
        totalTime = 0;
        groupDisposable = new CompositeDisposable(timerD),
        refCountDisposable = new RefCountDisposable(groupDisposable);

       function createTimer () ***REMOVED***
        var m = new SingleAssignmentDisposable(),
          isSpan = false,
          isShift = false;
        timerD.setDisposable(m);
        if (nextSpan === nextShift) ***REMOVED***
          isSpan = true;
          isShift = true;
        ***REMOVED*** else if (nextSpan < nextShift) ***REMOVED***
            isSpan = true;
        ***REMOVED*** else ***REMOVED***
          isShift = true;
        ***REMOVED***
        var newTotalTime = isSpan ? nextSpan : nextShift,
          ts = newTotalTime - totalTime;
        totalTime = newTotalTime;
        if (isSpan) ***REMOVED***
          nextSpan += timeShift;
        ***REMOVED***
        if (isShift) ***REMOVED***
          nextShift += timeShift;
        ***REMOVED***
        m.setDisposable(scheduler.scheduleWithRelative(ts, function () ***REMOVED***
          if (isShift) ***REMOVED***
            var s = new Subject();
            q.push(s);
            observer.onNext(addRef(s, refCountDisposable));
          ***REMOVED***
          isSpan && q.shift().onCompleted();
          createTimer();
        ***REMOVED***));
      ***REMOVED***;
      q.push(new Subject());
      observer.onNext(addRef(q[0], refCountDisposable));
      createTimer();
      groupDisposable.add(source.subscribe(
        function (x) ***REMOVED***
          for (var i = 0, len = q.length; i < len; i++) ***REMOVED*** q[i].onNext(x); ***REMOVED***
        ***REMOVED***,
        function (e) ***REMOVED***
          for (var i = 0, len = q.length; i < len; i++) ***REMOVED*** q[i].onError(e); ***REMOVED***
          observer.onError(e);
        ***REMOVED***,
        function () ***REMOVED***
          for (var i = 0, len = q.length; i < len; i++) ***REMOVED*** q[i].onCompleted(); ***REMOVED***
          observer.onCompleted();
        ***REMOVED***
      ));
      return refCountDisposable;
    ***REMOVED***, source);
  ***REMOVED***;
